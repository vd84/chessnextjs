import { Piece, Tile } from '../components/types'

export const enPassantPawn = async (
  board: Tile[],
  setPieceOnTile: (piece: Piece, pieceIdDestination: number, col: number, row: number) => void) => {

}
